

export class Category {
    id: number;
    name: string;
    attribute: string;

    constructor(id: number, name: string, attribute: string) {
        this.id = id;
        this.name = name;
        this.attribute = attribute;
    }
}